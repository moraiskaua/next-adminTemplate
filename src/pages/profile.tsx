import Layout from "../components/template/Layout";

interface ProfileProps {

}

const Profile = (props: ProfileProps) => {
    return (
        <Layout title='Perfil' subTitle="Seu perfil.">
            
        </Layout>
    )
}

export default Profile;